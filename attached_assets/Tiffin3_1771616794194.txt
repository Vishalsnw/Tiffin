You are upgrading an existing React + Firebase SaaS web app called “TiffinFlow”.

The app is already functional and includes:
- Authentication
- Dashboard
- Customer management
- Payment tracking
- Pause system

This app will be converted into a WebView-based Android app.

Your task is to refine, optimize, and polish the existing app so that:

- It feels like a real Android business app
- It is smooth and touch-friendly
- It is simple for Indian home-based tiffin providers
- It avoids website-like UI patterns
- It is production ready

Do NOT rebuild from scratch.
Improve and refactor the existing structure.

--------------------------------------------------
1️⃣ FULL ANDROID APP FEEL TRANSFORMATION
--------------------------------------------------

Remove website-style layout patterns:

- Remove large desktop paddings
- Remove centered max-width containers
- Use full-width layout
- Use min-h-screen for full height
- Remove unnecessary white margins

Make layout edge-to-edge like a mobile app.

--------------------------------------------------
2️⃣ BOTTOM NAVIGATION (APP STYLE)
--------------------------------------------------

Replace sidebar with Android-style Bottom Navigation.

Tabs:
- Dashboard
- Customers
- Today
- Payments
- Settings

Requirements:

- Fixed bottom nav
- Height 56–64px
- Icon + label
- Active tab highlight
- Smooth transition between tabs
- No browser-style links

Content must scroll, bottom nav must remain fixed.

--------------------------------------------------
3️⃣ DASHBOARD REFINEMENT (MORNING CONTROL VIEW)
--------------------------------------------------

Redesign dashboard to show only essential info:

Top large cards:

- Total Meals Today
- Veg Count
- Non-Veg Count
- Paused Today
- Expiring Soon (3 days)
- Pending Payments

Remove unnecessary charts.
Keep it simple and bold.

Each card must:
- Large number
- Clean label
- Rounded corners
- Soft shadow
- Proper spacing
- Touch friendly

Add current date at top.
Add business name header.

--------------------------------------------------
4️⃣ ADD FLOATING ACTION BUTTON (FAB)
--------------------------------------------------

Add circular Floating Action Button at bottom right:

Primary action: Add Customer

Must:
- Be elevated
- Have ripple effect
- Be clearly visible
- Not overlap bottom nav

--------------------------------------------------
5️⃣ CUSTOMER MODULE IMPROVEMENTS
--------------------------------------------------

Replace plain tables with card-based layout.

Each customer card shows:

- Name
- Veg type badge
- Status badge (Active / Paused / Expired)
- Remaining days
- Balance amount

Add:

- Search bar
- Filter by area
- Filter by status
- Clean edit bottom sheet (not browser modal)
- Proper delete confirmation bottom sheet

Tapping a card opens full-screen detail view.

--------------------------------------------------
6️⃣ TODAY PAGE IMPROVEMENT
--------------------------------------------------

Top large text:

“Aaj X tiffin banana hai”

Below:

Simple list:
- Customer name
- Veg type
- Area
- Special note

Add area filter.

No complex grid.
Clean delivery-friendly layout.

--------------------------------------------------
7️⃣ PAYMENT MODULE REFINEMENT
--------------------------------------------------

Improve payment UI:

Show:
- Total pending amount (top card)
- List of customers with balance

Highlight:
- Overdue customers in red
- Expiring soon in orange

Add:

- Mark as Paid button
- Send Reminder button (generate WhatsApp-ready message)

No complex accounting UI.
Keep it simple.

--------------------------------------------------
8️⃣ PAUSE SYSTEM LOGIC VALIDATION
--------------------------------------------------

Ensure:

- Paused customers are excluded from daily meal count
- Subscription extends correctly
- Expired customers are not included in meal count
- Timezone issues handled correctly
- No double counting

Add visible pause badge on customer card.

--------------------------------------------------
9️⃣ LOADING & EMPTY STATES
--------------------------------------------------

Add:

- Skeleton loaders while fetching data
- Clean empty state screens
- CTA button in empty state
- Toast notifications for success actions
- Error message banners

Avoid blank screens.

--------------------------------------------------
🔟 TOUCH & MOBILE UX OPTIMIZATION
--------------------------------------------------

Ensure:

- Minimum button height 44px
- Large input fields
- No hover-only interactions
- No horizontal scroll
- Proper spacing between cards
- Forms in single column
- Sticky action button for Save

--------------------------------------------------
1️⃣1️⃣ WEBVIEW OPTIMIZATION
--------------------------------------------------

Ensure:

- No zoom required
- No horizontal overflow
- No content hidden behind status bar
- Add safe bottom padding above nav
- Remove pull-to-refresh behavior if possible
- Fast initial loading

Add meta theme color for Android.

--------------------------------------------------
1️⃣2️⃣ PERFORMANCE CLEANUP
--------------------------------------------------

- Remove console logs
- Remove unused components
- Reduce unnecessary re-renders
- Optimize state updates
- Lazy load pages if needed

App must feel fast on mid-range Android phones.

--------------------------------------------------
1️⃣3️⃣ FIREBASE SECURITY CHECK
--------------------------------------------------

Ensure:

- Each user sees only their own data
- Proper data separation
- No public read/write
- Clean data structure

--------------------------------------------------
FINAL GOAL
--------------------------------------------------

Transform the existing app into:

- Clean
- Fast
- Simple
- Android app-like
- Ready for WebView conversion
- Usable by Indian home-based tiffin providers

Do not over-engineer.
Do not add enterprise features.
Keep it minimal but professional.